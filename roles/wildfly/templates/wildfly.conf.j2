# General configuration for the init.d scripts,
# not necessarily for JBoss AS itself.

## Location of JDK	
JAVA_HOME="/usr/local/linkbynet/jdk8"

## Location of WildFly
JBOSS_HOME="{{ wildfly_dest }}/wildfly-9.0.2.Final"

## The username who should own the process.
JBOSS_USER=wildfly

## The mode WildFly should start, standalone or domain
JBOSS_MODE=standalone

## Configuration for standalone mode
JBOSS_CONFIG=standalone-full.xml

## The amount of time to wait for startup
STARTUP_WAIT=60

## The amount of time to wait for shutdown
SHUTDOWN_WAIT=60

## Location to keep the console log
JBOSS_CONSOLE_LOG="/var/log/wildfly/console.log"

## Additionals args to include in startup
JBOSS_OPTS="-Dorg.kie.server.id=kie-server -Dorg.kie.server.location=http://localhost:8080/kie-server/services/rest/server -Dorg.kie.server.controller=http://localhost:8080/kie-wb/rest/controller -Dorg.jbpm.server.ext.disabled=true -Dorg.uberfire.nio.git.ssh.host=0.0.0.0 -Dorg.uberfire.nio.git.ssh.port=8001 -Dorg.uberfire.nio.git.dir={{ wildfly_dest }}/wildfly-9.0.2.Final -Dorg.uberfire.nio.git.daemon.host=0.0.0.0"

